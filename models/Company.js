class Company {
    constructor(
        id,
        name,
        description,
        banner
    ){
        this.id = id,
        this.name = name,
        this.description = description,
        this.banner = banner
    }
}

export default Company